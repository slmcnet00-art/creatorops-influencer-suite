# CreatorOps 제품 기준서

이 문서는 CreatorOps의 제품 목적, 운영 흐름, 데이터 원칙을 정의하는 최상위 기준서다. 화면과 기능을 변경할 때는 이 문서와 데이터룸 정의를 먼저 갱신한다.

## 1. 제품 목적

CreatorOps는 여러 브랜드가 브랜드에 맞는 인플루언서를 발굴하고, 섭외·협업·콘텐츠 제작·성과 추적을 통해 조회수와 전환 KPI를 달성하도록 돕는 팀 단위 B2B SaaS다. 일반 크리에이터뿐 아니라 TikTok 공동구매 셀러의 대량 섭외와 반복 운영도 지원한다.

## 2. 핵심 운영 흐름

1. **캠페인**: 브랜드, 제품, 타깃, 국가, 채널, 예산, 목표 인원, KPI, 판매/랜딩 URL을 등록한다.
2. **발굴**: 캠페인 브리프를 기준으로 실제 후보를 검색하고 AI가 적합도와 추천 근거를 계산한다.
3. **후보 그룹**: 검색 결과를 메시지 전 후보 풀 또는 재사용 그룹으로 저장하고 캠페인에 배정한다.
4. **메시지**: 이메일 주소가 검증된 후보는 승인 후 이메일로 발송하고, 나머지는 DM 작업 목록으로 관리한다.
5. **리포트**: 업로드 URL을 등록해 조회수·반응·전환·매출을 추적하고 보고서를 생성한다.
6. **레퍼런스**: 경쟁 브랜드와 콘텐츠를 조사·저장·분석하고, 승인된 구조만 다음 가이드 학습자료로 사용한다.
7. **데이터룸·어드민**: raw 데이터, 계산지표, 출처, 품질, 권한, 프롬프트와 운영 규칙을 관리한다.

## 3. 데이터 불변 원칙

- 프론트에 표시하는 수치와 상태는 반드시 데이터룸의 raw 데이터 또는 계산지표와 연결한다.
- 출처가 없거나 계산할 수 없는 값은 확정값처럼 표시하지 않는다. `수집 대기`, `검증 필요`, `수동 입력`으로 구분한다.
- 데이터 계보는 `원천 raw → 정규화 데이터 → 계산 스냅샷 → 화면/보고서` 순서로 추적 가능해야 한다.
- API raw, 외부 리포트 raw, 수동 업로드 raw, AI 생성 결과를 서로 다른 원천 유형으로 보존한다.
- 브라우저 `localStorage`는 임시 캐시일 뿐 운영 데이터의 원본 저장소가 아니다.
- 모든 운영 데이터에는 `workspace_id`와 `brand_id`를 부여해 팀·브랜드 권한을 격리한다.
- 공개 API와 승인된 연동을 우선한다. 플랫폼 제한을 우회하는 크롤링이나 사람처럼 위장한 DM 자동 발송은 사용하지 않는다.
- AI 생성물은 사용한 프롬프트 버전, 학습자료 버전, 생성 시각과 입력 캠페인을 기록한다.

## 4. 권한 모델

| 역할 | 기본 범위 |
| --- | --- |
| Owner | 팀, 계정, 권한, 전체 브랜드, 삭제와 초기화 |
| Admin | 브랜드·캠페인 운영, 데이터 다운로드, 운영 설정 |
| Manager | 배정 브랜드의 캠페인, 발굴, 메시지, 리포트 운영 |
| Marketer | 배정 캠페인의 실행과 콘텐츠 관리 |
| Analyst | 데이터 품질, 계산지표, 리포트 조회 |
| Client | 배정 브랜드의 컨펌 풀과 리포트 조회 |

권한은 계정 역할과 브랜드 배정을 함께 검사한다. 같은 팀이라도 배정받지 않은 브랜드의 데이터는 볼 수 없다.

## 5. 기능 기준

### 캠페인

- 전략 생성과 인플루언서 가이드 생성은 별도 실행한다.
- 공통 가이드와 크리에이터별 개별 가이드를 구분한다.
- 제품·브랜드 학습자료는 파일, Excel, Google Sheet로 연결할 수 있다.
- 판매/랜딩 URL은 캠페인 단위로 한 번 입력하고 크리에이터별 UTM과 숏링크를 파생한다.

### 발굴과 AI 추천

- 플랫폼, 국가, 카테고리, 팔로워, 평균 조회수, 참여율, 예상 단가를 필터링한다.
- 팔로워 수보다 평균 조회수와 팔로워 대비 터진 콘텐츠 비율을 우선 평가한다.
- AI 추천에는 브랜드 적합성, 콘텐츠 성과, 데이터 신뢰도, 위험 신호와 추천 이유를 함께 저장한다.
- 예시 후보와 실제 검색 후보를 섞지 않는다.

### 후보 그룹

- 검색 결과 풀: 대량 탐색 결과.
- 메시지 전 후보 풀: 실제 연락 대상으로 선별한 후보.
- 재사용 후보 그룹: 캠페인 간 다시 사용할 운영 자산.
- 그룹별 멤버, 플랫폼, 국가, 팔로워, 평균 조회수, 참여율, 예상 단가와 실제 협의 단가를 표시한다.

### 메시지

- 자동 발송 가능은 `검증된 이메일 주소 + 연결된 이메일 공급자 + 승인된 메시지`가 모두 충족된 경우에만 표시한다.
- 이메일이 없으면 자동 발송 가능으로 표시하지 않고 DM 작업 대상으로 분류한다.
- 검토함, 발송 완료, 응답 상태는 상호 배타적인 운영 상태로 필터링한다.
- 중복 발송, 수신 거부, 발송 한도, 오류와 응답 이력을 기록한다.
- 일반적인 캠페인 거절·제외는 해당 캠페인에서만 자동 발송을 차단한다. 수신자가 앞으로의 이메일을 명시적으로 거부한 경우에만 워크스페이스의 모든 캠페인에서 차단하며, 권한이 있는 운영자가 각각 해제할 수 있다.

### 리포트와 전환

- 콘텐츠 URL 등록 후 플랫폼, 크리에이터, 공개 지표를 자동 채우고 부족한 값만 수정하게 한다.
- 콘텐츠별 조회수, 좋아요, 댓글, 공유, 저장, 참여율과 일별 스냅샷을 보존한다.
- 성과가 검증된 자사 콘텐츠는 터진 이유와 재사용 구조를 분석해 템플릿으로 저장한다.
- UTM 스키마는 `utm_source`, `utm_medium`, `utm_campaign`, `utm_content`, `creator_name`, `content_url`, `cost`를 기본으로 한다.
- 실제 매출 추적은 GA4 또는 커머스 주문 데이터 연동 후 확정값으로 표시한다.

### 레퍼런스

- 브랜드 검색·추적과 개별 콘텐츠 추적을 분리한다.
- 검색 시에는 원천 데이터만 수집하고, AI 분석은 사용자가 저장한 레퍼런스에만 실행해 비용을 통제한다.
- 가이드 차용은 즉시 가이드를 생성하지 않는다. 승인된 후킹, 컷 구조, CTA, 표현 원칙을 다음 가이드 생성의 학습자료로 저장한다.

## 6. 데이터룸 기준

### Raw 원천 유형

- 내부 운영 raw: 브랜드, 캠페인, 후보, 그룹, 메시지, 계약, 배송, 수동 정산, 가이드, UTM.
- API raw: YouTube, Instagram, TikTok, 검색, 이메일, 환율, 분석·생성 공급자 응답.
- 리포트 raw: 브랜드 모니터, Video Monitor Data, Workbench, 광고주 제공 Excel.
- 수동·번외 raw: 공식 API로 얻지 못해 승인된 파일·URL·수동 검증으로 보완한 데이터.

각 raw 항목은 ID, 원천, 수집 방식, 주기, 최근/다음 수집, 상태, 저장 위치, 품질 이슈, 담당자, 로그 위치와 연결 지표를 가져야 한다.

### 계산지표

- 내부: CRM 효율, 인플루언서 풀, 캠페인 운영, 계약·수동 정산.
- 외부: SNS 반응, 콘텐츠 성과, 레퍼런스·벤치마크, 전환·매출.
- 상태는 `정상`, `지연`, `오류`, `검증 필요`로 관리한다.
- 예상 단가와 예상 조회수는 계산식과 입력 raw ID를 공개하고 실제 값으로 수정할 수 있어야 한다.

## 7. 어드민 운영 원칙

- 어드민에서 AI 추천 기준, 전략·가이드 프롬프트, 첨부 학습자료, UTM 규칙, 리포트 수집, 레퍼런스 수집, 계정·권한을 관리한다.
- 설정 변경은 즉시 기존 결과를 덮어쓰지 않는다. 버전을 발행한 뒤 이후 실행부터 적용한다.
- 변경자, 변경 시각, 이전 값, 새 값과 적용 범위를 감사 로그로 저장한다.

## 8. 완료 기준

- **UI 구현**: 화면은 존재하지만 서버·권한·실데이터 검증 전.
- **연동 구현**: 서버와 저장소가 연결되어 동일 계정이 다른 컴퓨터에서도 같은 데이터를 본다.
- **운영 가능**: 권한, 장애, 로그, 보안, 백업과 E2E 테스트까지 통과했다.

세 단계가 섞이지 않도록 기능정의서와 배포 체크리스트에 실제 상태를 기록한다.
